/* eslint-disable prettier/prettier */
/**
 * Method Core Extensions
 *
 * Adds Method's responsive spacing and typography controls
 * to targeted core WordPress blocks. This is not a block —
 * it's a set of filters that extend existing core blocks.
 */

import './modules/attributes';
import './modules/controls';
