/**
 * Method Fitted Image — Frontend
 *
 * Lightbox for fitted image blocks with the `lightbox` attribute enabled.
 * The implementation lives in lib/blocks/utils/lightbox.js so the Swiper
 * Gallery block can share the exact same dialog and behaviour.
 */
import { initMethodLightbox } from '../../utils/lightbox';

initMethodLightbox();
